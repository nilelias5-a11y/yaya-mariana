import RegistroForm from "./registro-form";

export const metadata = {
  title: "Crear cuenta · Mi cuenta",
  robots: { index: false, follow: false },
};

export default function CuentaRegistroPage() {
  return <RegistroForm />;
}
