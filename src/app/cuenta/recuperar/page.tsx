import RecuperarForm from "./recuperar-form";

export const metadata = {
  title: "Recuperar acceso · Mi cuenta",
  robots: { index: false, follow: false },
};

export default function CuentaRecuperarPage() {
  return <RecuperarForm />;
}
